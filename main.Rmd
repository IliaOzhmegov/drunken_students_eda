---
title: "EDA of Alcohol Consumption among Students"
author: Ilia Ozhmegov, Olena Horyn
date: "`r format(Sys.time(), '%B %Y')`"
output: 
  pdf_document:
    df_print: kable
    highlight: pygments
    fontsize: 11pt
    number_section: true
    toc: yes
    fig_caption: true
  html_document:
    df_print: paged
    highlight: pygments
    fontsize: 11pt
    number_section: true
    toc_depth: 2
    toc_float: true
    toc: yes
    fig_caption: true
    theme: cosmo
---

```{r Setting template options, include=FALSE}

knitr::opts_chunk$set(message=TRUE, warning = TRUE, echo = TRUE)
knitr::opts_chunk$set(fig.show = "hold", fig.align = "center")

```


```{r Loading libraries, include=FALSE}

library(aider)
library(tidyverse)

source("load_data.R")

```

Introduction text.  Introduction text.  Introduction text.  Introduction text.  Introduction text.  Introduction text.  Introduction text.  Introduction text.  Introduction text.  Introduction text.  Introduction text.  Introduction text.  Introduction text.  Introduction text.  Introduction text.  Introduction text.  Introduction text.  Introduction text.  Introduction text.  Introduction text.  Introduction text.  Introduction text.  Introduction text.  Introduction text. 

# Untitled section

Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. 

```{r}
math_df %>% 
  ggplot2::ggplot(aes(x=age, y=G1, group=age)) + ggplot2::geom_boxplot() +
  ggplot2::xlab("Age") + ggplot2::ylab("Grade for the first period") +
  ggtitle("Ушки Хрушки", subtitle = waiver()) +
  ggplot2::scale_x_continuous(breaks = seq(min(math_df$age), max(math_df$age), by = 1)) +
  ggplot2::scale_y_continuous(breaks = seq(min(math_df$G1), max(math_df$G1), by = 1))

```

# Another untitled section

Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. Здесь могла быть ваша реклама. 

```{r}

port_df %>% 
  ggplot2::ggplot(aes(x=age, y=G1, group=age)) + ggplot2::geom_boxplot() +
  ggplot2::xlab("Age") + ggplot2::ylab("Grade for the first period") +
  ggtitle("Крутилки свинки", subtitle = waiver()) +
  ggplot2::scale_x_continuous(breaks = seq(min(port_df$age), max(port_df$age), by = 1)) +
  ggplot2::scale_y_continuous(breaks = seq(min(port_df$G1),  max(port_df$G1), by = 1))

```